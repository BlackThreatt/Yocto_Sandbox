# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-sandbox-project"
BBFILE_PATTERN_meta-sandbox-project = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-sandbox-project = "6"

LAYERDEPENDS_meta-sandbox-project = "core"
LAYERSERIES_COMPAT_meta-sandbox-project = "scarthgap"
