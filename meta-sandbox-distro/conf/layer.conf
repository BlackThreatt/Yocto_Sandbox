# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-sandbox-distro"
BBFILE_PATTERN_meta-sandbox-distro = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-sandbox-distro = "6"

LAYERDEPENDS_meta-sandbox-distro = "core"
LAYERSERIES_COMPAT_meta-sandbox-distro = "scarthgap"
