header:
  version: 14
  includes:
    - kas/include/common.yml

machine: stm32mp25-disco
target: core-image-minimal

repos:
  meta-sandbox-bsp:
    path: meta-sandbox-bsp

  meta-sandbox-distro:
    path: meta-sandbox-distro

  meta-sandbox-project:
    path: meta-sandbox-project

env:
  INITM: "systemd"
  PKG_EXCLUDE: " "